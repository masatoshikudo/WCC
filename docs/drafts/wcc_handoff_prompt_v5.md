# For Your Wedding Day (WCC) — 引き継ぎプロンプト v5

> **作成日**: 2026-05-08
> **前回セッション**: 2026-05-07 第3回セッション継続(Phase B 全完了)
> **ステータス**: Phase B 完了、Phase C / Phase D / Phase E / `/packages` 処理が候補

---

## 役割分担(三者)

このプロジェクトは三者役割分担で進行している:

- **Claude Desktop(設計役)**: Masatoshi との対話、設計判断、Claude Code 向け指示書の起草、コピー本体の起草
- **Claude Code(実装役)**: Desktop が起草した指示書に従ったコード実装、コミット作成
- **Masatoshi(判断役)**: 事業判断、ブランチ運用、push、PR 作成、マージ、レビュー

**運用ルール**:
- Claude Code への指示には「コミットする」を含める(`userMemories` 記載)
- push / PR 作成 / マージは Masatoshi が手動で行う
- CLAUDE.md の PR 分割ルール: コード修正とドキュメント追加は別 PR

---

## プロジェクト概要

**サービス名**: For Your Wedding Day(WCC = Wedding Contents Creator)

**ポジション**: 株式会社 NaTRIUM(Masatoshi の一人会社)の事業ライン。SNS 運用代行(月額 50 万、本命事業)の前段「お試し撮影」の実績作りとして機能する。

**スタンダードプラン仕様(2026-05-07 確定、Phase B でマージ済み)**:
- 価格: **176,000 円(税込)**
- 撮影時間: **4 時間連続**
- スタッフ: 1 名
- 機材: iPhone または DJI Osmo Pocket
- 納品: 24 時間以内、9:16 縦動画 2 本 + 全素材

**Extras 5 項目**:
1. 撮影時間延長 +33,000 円 / 1 時間
2. 追加編集動画 +33,000 円 / 1 本
3. WCC スタッフ追加 +132,000 円 / 1 名(時間内固定)
4. 機材貸出(DJI Osmo Pocket)+38,500 円 / 1 台、最大 2 台
5. 遠方出張費 実費 + 11,000 円

**Bespoke(オーダーメイド)**: 330,000 円(税込)〜
- 共通項: 基本 4 時間撮影 + クリエイター 1 名(スタンダードと同じ)
- 可変部分: メニューにないご要望に応じた個別対応
- 例: 結婚式以外のフォトジェニックな日常シーンの撮影、特定シーンへの密着強化、ご家族向けの別動画制作、企業イベントとの連動、複数会場移動を含む工程設計

**業務フロー(v2)**:
/book フォーム → メールで見積もり → Stripe 請求書 URL → 決済 → 公式 LINE → ZOOM 詳細打ち合わせ → 撮影当日 → 24 時間以内納品

---

## Phase B 完了サマリー(2026-05-07 完了)

LP 全体を v2 業務フロー + 4h スタンダード + Extras 5 項目 + Bespoke 330k(税込)〜 に統一。**6 PR、すべてマージ済み**。

| Step | PR 内容 | 主な変更 |
|---|---|---|
| **B-0** | 法的リスクホットフィックス | `tokushoho-draft.ts` の旧価格 150,000 円 → 160,000 円(税抜)、プラン名「パッケージ」→「スタンダード」 |
| **B-1 + B-2** | plans.ts + home-faq.ts 単一ソース化 | `WCC_PAYMENT_TIMING_NOTE` を v2 フローに、`plan.items` 4h 化、`WCC_EXTRAS` 定数新規追加、FAQ 4 件書き換え(支払いタイミング / 日程確定 / ZOOM 内容 / 機材説明) |
| **B-3** | HomeServiceFlowSection PHASES 再構成 | 8 ステップ / 3 フェーズ(ご相談・お見積もり 1-3 / ご契約 4-6 / 準備〜当日 7-8)。ZOOM の位置を「決済前の相談」から「決済後の詳細打ち合わせ」に移動 |
| **B-1.5 + B-4 + B-5** | /pricing v2 化 + Extras 5項目化 + Bespoke 330k + BookFlow 説明文 | `WCC_EXTRAS` に `description` フィールドと `formatExtraPrice()` ヘルパー追加。/pricing メタ・ヘッダ帯コピー修正、Extras を `WCC_EXTRAS` 参照に切替、Bespoke 220,000 → 330,000 円、BookFlow.tsx:212 説明文を v2 フローに |
| **B-5 完成** | HomePricingSection compact モード | 「6時間撮影」→「4時間撮影」(B-5 修正漏れ補完) |
| **B-6** | コピーガイド v3 → v4 大改訂 | 800 行超。新セクション 4「事業フローとコピーの責務区分」、サブセクション 7.1「表記統一規定」、サブセクション 14.1「/pricing 固有のセクション方針」追加。Phase B 由来の禁句・コピー例反映 |

---

## 確定した設計知見(Phase B で得られたもの)

### 1. `WCC_EXTRAS` データ構造(`site/lib/plans.ts`)

```typescript
export const WCC_EXTRAS = [
  {
    id: 'extension',
    name: '撮影時間延長',
    priceYen: 33000,
    unit: 'time',
    note: '※2 名体制時は +25% 加算',
    description: '標準 4 時間に追加',
    maxQuantity: null,
  },
  // ... 他 4 項目
] as const;
```

各フィールドの責務:
- `id`: Phase C で /book フォームから参照する識別子
- `name`: 表示用項目名
- `priceYen`: 数値、`formatExtraPrice()` でフォーマット
- `unit`: `'time' | 'video' | 'person' | 'unit' | 'flat'`、表示形式の分岐に使用
- `note`: 業務メモ(料金体系などの注記)
- `description`: /pricing 表示用説明文
- `maxQuantity`: 数量上限、`null` は無制限(Phase C で null チェック必須)

### 2. `formatExtraPrice()` ヘルパー

`unit` ごとに表示文字列を生成:
- `time` → "+33,000 円 / 1 時間"
- `video` → "+33,000 円 / 1 本"
- `person` → "+132,000 円 / 1 名"
- `unit` → "+38,500 円 / 1 台"
- `flat` → "実費 + 11,000 円"(例外パターン)

`never` 型による網羅性チェックを含むため、将来 `unit` が増えた場合はコンパイルエラーで気付ける設計。

### 3. スタッフ追加時の延長料金

- 1 名体制延長: 33,000 円/h
- 2 名体制延長: 41,250 円/h(+25%)
- 3 名体制延長: 49,500 円/h(+50%)

Phase C の自動計算実装で必要になる。

### 4. コピーガイド v4 の責務区分(セクション 4 で明文化)

- **WCC_PRODUCT.md**: 業務工程の順序・実行内容(単一ソース)
- **コピーガイド**: 業務工程を表現する言い回し規定
- **業務文テンプレート**: 別途検討事項(現状 WCC_PRODUCT.md 内に簡易記載)

---

## 残課題

### 1. `/packages` ページの処理(優先度: 中)

**問題**:
- `/pricing` に統合済みの旧 legacy ページが残存
- 食事手配 FAQ に "6時間" が残っている
- 中身が旧フロー前提(無料 ZOOM 相談、契約前打ち合わせ等)

**推奨対応**:
- 削除 + `/pricing` への 301 リダイレクト設定
- 既存被リンク・SEO の影響確認
- 独立した小規模 Phase として処理可能(他フェーズと並行可)

### 2. WCC_PRODUCT.md v2 のリポジトリ配置状況確認

第3回セッション時点で起草・コミット済みの認識だが、実際のリポジトリ配置・最新性を改めて確認したい(コピーガイド v4 が WCC_PRODUCT.md を「単一ソース」として参照しているため)。

### 3. Phase C 着手前に決めたい設計事項

- **/book フォームの自動計算ロジック**: スタッフ追加時の +25% / +50% ルール、上限値、合計計算式
- **自動メール文面**: 引き継ぎ v4 で起草済みの文面に「項目を入力いただくと、概算料金が自動表示されます。」を追加するタイミング(Phase C 完了時)

---

## 次フェーズの候補

### Phase C: `/book` フォーム改修(規模: 大)

**目的**: Extras 項目追加、自動計算機能、自動メール文面更新

**想定 Step**:
- C-1: フォーム UI に Extras 5 項目の入力欄追加(チェックボックス + 数量入力)
- C-2: クライアントサイドで自動計算ロジック実装(`formatExtraPrice` 拡張版)
- C-3: 自動メール文面更新(送信処理サーバーサイド)
- C-4: `/book` 説明文に「概算料金が自動表示されます」を追加(B-5 で意図的に外していた)

**Phase B で先回り済みの基盤**:
- `WCC_EXTRAS` 単一ソース定義済み
- `formatExtraPrice()` 関数実装済み
- 型定義(`WccExtra`)エクスポート済み

### Phase D: Stripe / メール / LINE 連携整備(規模: 中〜大)

**目的**: 業務フロー(フォーム → 見積 → 請求書 → 決済 → LINE → ZOOM)の自動化

- 自動メール送信(問い合わせ → 見積もり)
- Stripe Checkout / Payment Link 連携
- 決済完了 webhook → 公式 LINE 招待自動化(or 手動運用設計)

### Phase E: 既存ドキュメント整合性(規模: 小〜中)

**目的**: WCC_BRAND.md 確認、その他ドキュメントの v2 仕様整合

- `WCC_BRAND.md` の v2 仕様反映状況確認
- 古いドキュメント(`WCC_IDEA.md` のアーカイブ扱い等)の整理
- `docs/drafts/` 配下のドラフト群の棚卸し

### `/packages` 整理(規模: 小)

独立した小規模 Phase として処理。上記いずれのフェーズとも並行可。

---

## 推奨着手順

事業価値 × 技術依存性の観点から:

1. **Phase C** を最優先(/book フォームが完全動作 = サービス本体が完成)
2. **/packages 整理**を Phase C と並行で消化(小規模、独立)
3. **Phase D** を Phase C 完了後に着手(決済が動かないとサービス起動不可だが、Phase C 完了で「仮予約 + 手動決済」運用は可能)
4. **Phase E** は最後でよい(整理作業、リリースに必須ではない)

ただし Masatoshi の事業優先度次第で順序変更可。

---

## 次セッション開始時の最初のアクション

引き継ぎプロンプトを Desktop に貼り付けた直後、以下を確認する:

1. **WCC_PRODUCT.md v2 のリポジトリ配置状況**
   - `docs/` 配下に最新版が存在するか
   - 最終更新日とコピーガイド v4 との整合

2. **次フェーズの選択**
   - 推奨着手順(Phase C → /packages 整理 → Phase D → Phase E)で進めるか、別の優先度か

3. **着手フェーズの事前情報収集**
   - Phase C を選ぶ場合: 現状の `/book` ページ全体構造(`BookFlow.tsx` 全文 + 関連コンポーネント)
   - `/packages` を選ぶ場合: `/packages/page.tsx` 全文 + 既存被リンク状況
   - Phase D を選ぶ場合: 既存の Stripe / メール / LINE 連携状況、API キー管理体制
   - Phase E を選ぶ場合: `WCC_BRAND.md` 全文

---

## 過去セッション参照リンク

- 第1回 / 第2回 / 第3回セッションの判断記録は各 PR コミットメッセージに記録済み
- 引き継ぎプロンプト v4(2026-05-07 開始時): リポジトリの過去版または `wcc_handoff_prompt_v4.md` を参照
- コピーガイド v4 全文: `docs/drafts/WCC_COPY_GUIDE.md`(本セッションでマージ済み)

---

## 重要な事業設計判断のリマインダー

### Bespoke の位置付け

Bespoke は「Extras を積み上げた延長線」ではなく、**スタンダードと SNS 運用代行(月額 50 万)の架け橋**。基本構造(4 時間撮影 + クリエイター 1 名)はスタンダードと同じだが、「メニューにないご要望」に応じた個別対応が乗ることで価格差(176,000 → 330,000 円)が生まれる。

「初期設計」という業務専門用語は表に出さない方針(コピーガイド v4 セクション 14.1 参照)。

### 「無料」を約束しない原則

業務フロー変更(無料 ZOOM 相談廃止)以降、コピー上での「無料」明示は避ける。/book フォームでの問い合わせ・お見積もりは無償だが、これを「無料相談」と表現すると旧フローを連想させる。「日程未定の段階からご相談いただけます」のように別の言い方でハードルの低さを表現する。

### コピー全体の温度設定

- LP トップ: 高温・詩的(ヒーロー)/ 高温・解説(What is、Do I need)/ 中温(Pricing、OUR WORK)/ 低温・明快(service-flow、FAQ)
- /pricing: 中温・口語(ヘッダ帯)/ 中温・正確(Pricing)/ 低温(Extras、FAQ、service-flow)/ 中温・解説(Bespoke)
- 二人称: 詩的 = 「2人」 / 本文 = 「ふたり」 / 業務文 = 「お二人」 / 「あなた」禁止

詳細はコピーガイド v4 を参照。

---

*作成: 2026-05-08(Phase B 完了直後)*
*ステータス: Phase B 完了、次フェーズ未着手*
